name: Build App

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upload files
        uses: actions/upload-artifact@v4
        with:
          name: app-files
          path: .
